services:
  - type: web
    name: pirate-dice-backend
    runtime: node
    buildCommand: npm install --include=dev && npm run build:backend
    startCommand: npm run start:backend
    envVars:
      - key: NODE_ENV
        value: production
      - key: FRONTEND_URL
        value: https://pirate-dice-game.web.app
